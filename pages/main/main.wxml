<!--main.wxml-->
<view>

    <view wx:if="{{hidden == false}}"> 
      <include src="../../components/loading.wxml" /> 
    </view>

    <!-- <view data-publish-time="{{item.time}}" bindtap="onItemClick">
    <video id="myVideo" class="image" src="{{video_url}}" controls objectFit="fill" poster="http://owgx5je1w.bkt.clouddn.com/17-10-10/70340367.jpg"></video>
            <text class="time">time</text>
            <view class="title">title</view>
        </view>

        <view data-publish-time="{{item.time}}" bindtap="onItemClick">
    <video id="myVideo" class="image" src="{{video_url}}" controls objectFit="fill" poster="http://owgx5je1w.bkt.clouddn.com/17-10-10/70340367.jpg"></video>
            <text class="time">time</text>
            <view class="title">title</view>
        </view>
    <view class="card">
        <view data-publish-time="{{item.time}}" bindtap="onItemClick">
    <video id="myVideo" class="image" src="{{video_url}}" controls objectFit="fill" poster="http://owgx5je1w.bkt.clouddn.com/17-10-10/70340367.jpg"></video>
            <text class="time">time</text>
            <view class="title">title</view>
        </view>
        </view> -->


    <!-- <view class="card" wx:for="{{items}}">
        <view data-publish-time="{{item.time}}" bindtap="onItemClick">
            <image class="image" mode="aspectFill" src="{{item.src}}"/>
            <text class="time">{{item.time}}</text>
            <view class="title">{{item.title}}</view>
        </view>
    </view> -->
    <view class="card" wx:for="{{items}}">
      <view data-publish-time="{{item.time}}" bindtap="onItemClick">
      <video id="myVideo" class="image" src="{{item.video}}" controls objectFit="fill" poster="{{item.image}}"></video>
      <text class="time">{{item.name}}</text>
      <view class="title">{{item.description}}</view>
      </view>
    </view>

    <!-- <icon type="success" size="50" class="post" bindtap="onPostClick"/> -->
</view>
